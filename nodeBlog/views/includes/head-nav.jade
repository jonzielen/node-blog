mixin link(href, name)
  li
    a(href=href)&attributes(attributes)= name

nav#main-nav
    ul
        if !homepage
            +link('/', 'Home')(class="btn")

        +link('/blog', 'My Blog')(class="btn")
